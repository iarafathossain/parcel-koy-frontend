export interface IHub {
  id: string;
  name: string;
  slug: string;
  address: string;
  contactNumber: string;
  isActive: boolean;
  managerId: string;
  createdAt: string;
  updatedAt: string;
}
